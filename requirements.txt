# Sentinel — AI Delivery Intelligence Layer. Python 3.12+. Host-native (no Docker for dev).
# Direct dependencies only; neuro-san / nsflow pull their own transitives.
# Install: python -m venv .venv && .venv/Scripts/pip install -r requirements.txt

# --- Neuro-SAN framework (sole multi-agent orchestrator) ---
neuro-san==0.6.70
nsflow==0.6.16
leaf-common>=1.2.44

# --- Config parsing ---
pyhocon>=0.3.60
python-dotenv>=1.2.2,<2.0.0
PyYAML>=6.0

# --- LLM: NVIDIA NIM primary (locked design). nvidia class lazy-loads this. ---
langchain-nvidia-ai-endpoints>=1.4.3,<2.0
# Optional fallback providers — uncomment only if FALLBACK_MODEL_NAME targets them:
# langchain-openai>=1.1.9,<2.0
# langchain-anthropic>=1.4.3,<2.0
# langchain-google-genai>=4.2.2,<5.0

# --- Database (PostgreSQL 17, psycopg3 driver) ---
SQLAlchemy>=2.0,<3.0
alembic>=1.18
psycopg[binary]>=3.3

# --- Data contracts ---
jsonschema>=4.0

# --- Dev / test ---
pytest>=8.0
